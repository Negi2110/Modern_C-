// unordered_map which is very similar to map but offers average-cast constant time insertion lookup, and deletion.
// The difference with this data structure (versus a std::map) is that the underlying data structure is a hash_table (similar to unordered_set versus set) versus a red-black tree.
#include <iostream>
#include <unordered_map>

int main()
{

    return 0;
}